<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.voicerecognitioncalculator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/calc_logo1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/calc_logo1"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceRecognitionCalculator">
        <activity
            android:name=".SampleCalcActivity"
            android:exported="false" />
        <activity android:name=".HistoryCalcActivity"
            android:exported="false"/>
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>